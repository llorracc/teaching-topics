# Workflow: Install the environment (via Cursor)

**Purpose:** Use Cursor to install the Python/conda environment for the ballpark repo.

---

## Steps

1. In Cursor, ask:
   > "How do I install the environment that will let me execute the code in this repo?"
2. Follow Cursor's instructions to install the environment.
3. Execute the installation steps.
