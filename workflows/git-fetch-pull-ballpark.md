# Workflow: Git fetch/pull on ballpark

**Purpose:** Update your local ballpark clone to the latest version.

---

## Steps

1. `cd` to your local clone of the ballpark repo.
2. Run:
   ```bash
   git fetch
   git pull
   ```
