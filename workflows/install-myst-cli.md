# Workflow: Install MyST (CLI)

**Purpose:** Install the MyST command-line tools for building and converting documents.

---

## Steps

1. Ask Cursor:
   > "How do I install MyST?"
2. Follow the instructions to install MyST (typically `pip install mystmd` or `npm install -g mystmd`).
