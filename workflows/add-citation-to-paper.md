# Workflow: Add a citation to the paper

**Purpose:** Add a proper citation to the paper itself in your notebook/MyST document.

---

## Steps

1. In the Jupyter notebook (or MyST document), add a **proper citation** to the paper itself using MyST citation syntax (e.g. `{cite}` or `[@key]`).
